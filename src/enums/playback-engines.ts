/** 
 * Playback engine types.
 * `TTS` - Text-to-Speech engine using Web Speech API.
 * `TRACKPLAYER` - Audio playback engine using TrackPlayer.
 */
export enum PlaybackEngineType {
    TTS = "tts",
    TRACKPLAYER = "tp"
}