import { DEFAULT_TARGET_LANGUAGE, DEFAULT_LANGUAGE_LEVEL, DEFAULT_NATIVE_LANGUAGE } from "@/constants/general";

export const defaultRequestLanguageProps = {
    targetLanguage: DEFAULT_TARGET_LANGUAGE,
    userLanguageLevel: DEFAULT_LANGUAGE_LEVEL,
    userNativeLanguage: DEFAULT_NATIVE_LANGUAGE,
}